# 🎰 Lucky Draw - ระบบสุ่มผู้โชคดี

ระบบเว็บแอปพลิเคชันสำหรับสุ่มผู้โชคดี พร้อม UI สวยงามเหมือนเล่นเกม!

## ✨ Features

- 🎡 วงล้อสุ่มรางวัลพร้อม Animation สวยงาม
- 🏆 รางวัลใหญ่ Grand Prize สำหรับ 1 รายชื่อ
- 👥 เลือกจำนวนผู้โชคดีได้
- 📊 หน้าดูผลรางวัล - ผู้ได้รับรางวัล/ยังไม่ได้รับ
- ⚙️ หน้าจัดการรายชื่อ (เพิ่ม/ลบ)
- 🎊 Confetti Animation เมื่อประกาศผู้โชคดี
- 💾 เก็บข้อมูลด้วย SQLite Database

## 🚀 วิธีติดตั้งและใช้งาน

### 1. ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

### 2. รันเซิร์ฟเวอร์

```bash
python app.py
```

### 3. เปิดเบราว์เซอร์

เข้าไปที่ http://localhost:5000

## 📁 โครงสร้างโปรเจค

```
Spin/
├── app.py              # Flask Backend
├── requirements.txt    # Python Dependencies
├── README.md          # เอกสารนี้
├── instance/
│   └── lucky_draw.db  # SQLite Database (สร้างอัตโนมัติ)
└── templates/
    ├── base.html      # Template หลัก
    ├── spin.html      # หน้าสุ่มรางวัล
    ├── results.html   # หน้าดูผลรางวัล
    └── admin.html     # หน้าจัดการรายชื่อ
```

## 🎮 วิธีใช้งาน

### 1. เพิ่มผู้เข้าร่วม
- ไปที่หน้า **จัดการ** 
- เพิ่มชื่อทีละคน หรือ เพิ่มหลายคนพร้อมกัน (หนึ่งชื่อต่อบรรทัด)

### 2. สุ่มรางวัล
- ไปที่หน้า **สุ่มรางวัล**
- เลือกจำนวนผู้โชคดี
- กด **หมุนสุ่มรางวัล** สำหรับรางวัลปกติ
- กด **สุ่มรางวัลใหญ่** สำหรับ Grand Prize (1 คน)

### 3. ดูผลรางวัล
- ไปที่หน้า **ผลรางวัล**
- ดูรายชื่อผู้ได้รับรางวัลและยังไม่ได้รับ

## 🛠️ API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/participants` | ดึงรายชื่อผู้เข้าร่วมที่ยังไม่ได้รางวัล |
| POST | `/api/participants` | เพิ่มผู้เข้าร่วมใหม่ |
| POST | `/api/participants/bulk` | เพิ่มผู้เข้าร่วมหลายคน |
| DELETE | `/api/participants/<id>` | ลบผู้เข้าร่วม |
| POST | `/api/spin` | สุ่มผู้โชคดี |
| POST | `/api/reset` | รีเซ็ตผลการสุ่ม |
| POST | `/api/clear-all` | ลบข้อมูลทั้งหมด |

## 💻 Tech Stack

- **Backend:** Python Flask
- **Database:** SQLite with SQLAlchemy ORM
- **Frontend:** HTML, CSS, JavaScript
- **Fonts:** Prompt (Thai), Orbitron (Display)
- **Icons:** Font Awesome 6

---

Made with ❤️ for Lucky Draw events!

